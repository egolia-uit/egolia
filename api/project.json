{
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "name": "api",
  "projectType": "library",
  "tags": ["scope:api", "type:openapi"],
  "targets": {
    "bundle:json": {
      "dependsOn": ["^bundle:json"]
    },
    "join": {
      "inputs": [
        {
          "dependentTasksOutputFiles": "api/bundled/*.json"
        },
        "!{projectRoot}/bundled/openapi.json"
      ],
      "outputs": ["{projectRoot}/bundled/openapi.json"],
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run redocly:join",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["^bundle:json"],
      "cache": true
    },
    "watch-join": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm nx watch --projects=api/note,api/document,api/event -- pnpm nx run api:join",
        "forwardAllArgs": false,
        "cwd": "{workspaceRoot}"
      },
      "continuous": true
    },
    "bundle": {
      "dependsOn": ["join"]
    },
    "watch-bundle:json": {
      "dependsOn": ["^watch-bundle:json"]
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "scalar document serve -w ./bundled/openapi.json --port 9080",
        "forwardAllArgs": false,
        "cwd": "{projectRoot}"
      },
      "continuous": true,
      "dependsOn": ["join", "watch-join"]
    },
    "lint": {
      "executor": "nx:noop",
      "dependsOn": ["^lint"]
    },
    "mock": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "command": "nx run-many --target=mock --projects=api/* --parallel"
      }
    },
    "mock:watch": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "command": "nx run-many --target=mock:watch --projects=api/* --parallel"
      }
    },
    "gen:go": {
      "dependsOn": ["^gen:go"]
    },
    "gen:hey-api": {
      "outputs": ["{workspaceRoot}/packages/api-gen/src/"],
      "inputs": [
        {
          "dependentTasksOutputFiles": "api/bundled/openapi.json",
          "transitive": true
        },
        "{projectRoot}/openapi-ts.config.ts"
      ],
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec openapi-ts",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["join"]
    },
    "gen": {
      "dependsOn": ["^gen:go", "gen:hey-api"]
    }
  },
  "implicitDependencies": ["api/course", "api/billing", "api/blog"]
}
