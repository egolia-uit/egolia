description: Test created
content:
  application/json:
    schema:
      allOf:
        - $ref: ../../../common/components/schemas/DataEnvelope.yaml
        - type: object
          properties:
            data:
              $ref: ../schemas/TestLesson.yaml
          required:
            - data
