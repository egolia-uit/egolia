description: Lesson progress
content:
  application/json:
    schema:
      allOf:
        - $ref: ../../../common/components/schemas/DataEnvelope.yaml
        - type: object
          properties:
            data:
              $ref: ../schemas/LessonProgress.yaml
          required:
            - data
