type: object
properties:
  section:
    allOf:
      - $ref: ./Section.yaml
      - type: object
        properties:
          lessons:
            type: array
            items:
              $ref: ./Lesson.yaml
        required:
          - lessons