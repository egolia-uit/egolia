type: object
allOf:
  - $ref: ./Section.yaml # Lấy id, title, position... từ file gốc
  - type: object
    properties:
      lessons:
        type: array
        items:
          $ref: ./Lesson.yaml
    required:
      - lessons
