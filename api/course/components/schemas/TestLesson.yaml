allOf:
  - $ref: ./Lesson.yaml
  - type: object
    properties:
      lessonType:
        type: string
        enum:
          - test
        example: test

      questionType:
        $ref: ./QuestionType.yaml
        readOnly: true

      questions:
        type: array
        items:
          type: object
          $ref: ./TestQuestion.yaml

    required:
      - lessonType
      - questionType
      - questions
