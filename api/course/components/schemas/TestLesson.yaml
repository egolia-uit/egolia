allOf:
  - $ref: ./Lesson.yaml
  - type: object
    required:
      - type
      - questions
    properties:
      type:
        type: string
        enum:
          - multipleChoice
          - singleChoice
        example: singleChoice

      questions:
        type: array
        items:
          type: object
          $ref: ./TestQuestion.yaml