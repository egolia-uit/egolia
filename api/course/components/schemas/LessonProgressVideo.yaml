allOf:
  - $ref: ./LessonProgress.yaml
  - type: object
    properties:
      watchedSeconds:
        type: number
        format: float
        minimum: 0
        nullable: true
        example: 125.5

      lastViewedAt:
        type: string
        format: date-time
        example: 2026-03-22T09:00:00Z

    required:
      - lastViewedAt
