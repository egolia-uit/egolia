required: true
content:
  application/json:
    schema:
      type: object
      required:
        - type
        - questions
      properties:
        type:
          $ref: ../schemas/TestLessonType.yaml
        questions:
          type: array
          minItems: 1
          items:
            $ref: ../schemas/TestQuestion.yaml
