# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
version: 1
metadata:
  name: Egolia - API Web
  labels:
    blueprint-group: egolia
    blueprints.goauthentik.io/description: |
      Egolia - API Web OAuth2 Provider and Application

entries:
  - id: provider
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      client_id: api-web
    attrs:
      name: API Web
      meta_launch_url: https://api.egolia.localhost
      client_secret: egoliauit
      client_type: public
      access_code_validity: minutes=10
      access_token_validity: days=30
      refresh_token_threshold: hours=1
      refresh_token_validity: days=90
      sub_mode: user_id
      authorization_flow:
        !Find [
          authentik_flows.flow,
          [slug, default-provider-authorization-implicit-consent],
        ]
      invalidation_flow:
        !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      signing_key:
        !Find [
          authentik_crypto.certificatekeypair,
          [name, authentik Self-signed Certificate],
        ]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
        - !Find [
            authentik_providers_oauth2.scopemapping,
            [scope_name, entitlements],
          ]
      redirect_uris:
        - url: ^http://api\.egolia\.localhost/.*$
          matching_mode: regex
        - url: http://api.egolia.localhost
          matching_mode: strict
        - url: ^http://localhost:9080/.*$
          matching_mode: regex
        - url: http://localhost:9080
          matching_mode: strict

  - model: authentik_core.application
    id: app
    identifiers:
      slug: api-web
    attrs:
      name: API Web
      provider: !KeyOf provider

  - model: authentik_core.applicationentitlement
    id: instructor-entitlement
    identifiers:
      name: instructor
    attrs:
      app: !KeyOf app

  - model: authentik_core.applicationentitlement
    id: admin-entitlement
    identifiers:
      name: admin
    attrs:
      app: !KeyOf app

  - model: authentik_core.group
    id: egolia-instructor
    state: created
    identifiers:
      name: Egolia Instructor

  - model: authentik_core.group
    id: egolia-admin
    state: created
    identifiers:
      name: Egolia Admin

  - model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf admin-entitlement
      group: !KeyOf egolia-admin

  - model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf instructor-entitlement
      group: !KeyOf egolia-instructor
